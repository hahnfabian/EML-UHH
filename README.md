# Programmieraufgaben aus EML

Für die Ausführung bietet sich eine eigene Conda-Umgebung an. Die benötigten Packages sind in der Datei `requirements.txt` zu finden.
```
conda create --name EML --file requirements.txt
``` 